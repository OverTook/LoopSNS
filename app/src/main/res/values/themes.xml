<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.Theme.LoopSNS" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/activity_background</item>
        <item name="android:navigationBarColor">@color/activity_background</item>

        <item name="md_corner_radius">10dp</item>
    </style>
  <!-- Base application theme. -->
    <style name="Base.Base.Theme.LoopSNS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>